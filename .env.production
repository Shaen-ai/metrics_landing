# Loaded for `next build` / `next start` (not for `next dev`).
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-NEC36L9NYL

# Required for Stripe “Get started” on /pricing — same Laravel API base as metrics_app (see checkout-urls.ts).
NEXT_PUBLIC_API_URL=https://api.tunzone.com/api
